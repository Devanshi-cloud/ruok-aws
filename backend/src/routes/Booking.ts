import express from 'express';
const router = express.Router();

// Define your booking routes here. For example:
// router.get('/', (req, res) => {
//   res.send('Booking routes are working!');
// });

export default router;